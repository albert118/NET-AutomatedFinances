/**
 * Avoid hardcoding router-paths all over the app! use this file 
 * as the source of truth
 */

const AppRoutes = {
    root: '/',
    addTransactionRecord: '/transactions',
    allTransactionRecords: '/all-transactions'
};


export default AppRoutes;